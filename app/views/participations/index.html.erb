<div class="row row-cards">
  <div class="col-lg-3">
    <div class="row">
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="col-lg-9">
    <div class="card">
      <table class="table card-table table-vcenter">
    <tbody>
      <% @participations.each do |participation| %>
      <tr>
            <td><%= participation.exhibition_id %></td>
            <td><%= participation.company_id %></td>
            <td><%= participation.year %></td>
            <td><%= link_to t(:show), participation %></td>
      <td><%= link_to t(:edit), edit_participation_path(participation) %></td>
      <td><%= link_to t(:destroy), participation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
</div>
